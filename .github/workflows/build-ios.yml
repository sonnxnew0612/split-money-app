name: Build iOS IPA (Unsigned)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18
        
    - name: Install Dependencies
      run: |
        rm -rf package-lock.json node_modules
        npm install
    
    # --- BƯỚC MỚI: SỬA LỖI THIẾU FILE Package.swift ---
    - name: Fix Missing Package.swift for CapacitorFirebaseAuthentication
      run: |
        echo "Downloading missing Package.swift for @capacitor-firebase/authentication..."
        # Di chuyển vào thư mục của thư viện
        cd node_modules/@capacitor-firebase/authentication
        
        # Tải file Package.swift từ GitHub chính chủ (nhánh main)
        curl -O https://raw.githubusercontent.com/capawesome-team/capacitor-firebase/main/packages/authentication/Package.swift
        
        # Kiểm tra xem file đã được tải về chưa
        ls -l Package.swift
        cat Package.swift | head -n 5
    # --------------------------------------------------
      
    - name: Build React App
      run: npm run build
      
    - name: Sync Capacitor
      run: npx cap sync ios

    - name: Link GoogleService-Info.plist to Xcode Project
      run: |
        gem install xcodeproj
        ruby -e '
          require "xcodeproj"
          project_path = "ios/App/App.xcodeproj"
          file_name = "GoogleService-Info.plist"
          file_path = "ios/App/App/GoogleService-Info.plist"
          project = Xcodeproj::Project.open(project_path)
          group = project.main_group["App"]
          unless group.files.find { |f| f.path == file_name }
            file_ref = group.new_file(file_name)
            target = project.targets.first
            target.add_resources([file_ref])
            project.save
            puts "Linked #{file_name} successfully!"
          else
            puts "#{file_name} already exists."
          end
        '
      
    - name: Build Archive (xcodebuild)
      run: |
        cd ios/App
        xcodebuild -project App.xcodeproj -scheme App -configuration Release -derivedDataPath build -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO clean build
        
    - name: Create IPA manually
      run: |
        mkdir -p Payload
        cp -r ios/App/build/Build/Products/Release-iphoneos/App.app Payload/
        zip -r SplitMoney.ipa Payload
        
    - name: Upload Artifact (IPA)
      uses: actions/upload-artifact@v4
      with:
        name: SplitMoney-iOS
        path: SplitMoney.ipa
